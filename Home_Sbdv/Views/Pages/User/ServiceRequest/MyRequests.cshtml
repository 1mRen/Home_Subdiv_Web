@model IEnumerable<Home_Sbdv.Models.ServiceReqViewModel>
@{
    ViewData["Title"] = "My Service Requests";
}

<div class="container-fluid px-4">
    <div class="card shadow-sm">
        <div class="card-header bg-transparent py-3 d-flex justify-content-between align-items-center">
            <h5 class="mb-0">My Service Requests</h5>
            <a asp-action="Create" class="btn btn-primary">New Request</a>
        </div>
        <div class="card-body">
            @if (Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>@Html.DisplayNameFor(m => m.First().RequestId)</th>
                                <th>@Html.DisplayNameFor(m => m.First().Request_Type)</th>
                                <th>@Html.DisplayNameFor(m => m.First().Description)</th>
                                <th>@Html.DisplayNameFor(m => m.First().Status)</th>
                                <th>@Html.DisplayNameFor(m => m.First().Submitted_at)</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var request in Model)
                            {
                                <tr>
                                    <td>@request.RequestId</td>
                                    <td>@request.Request_Type</td>
                                    <td>@request.Description</td>
                                    <td>
                                        <span class="badge @request.GetStatusBadgeClass()">@request.Status</span>
                                    </td>
                                    <td>@request.GetFormattedSubmissionDate()</td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@request.RequestId" class="btn btn-sm btn-outline-primary">View Details</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <div class="mb-3">
                        <i class="bi bi-inbox text-muted" style="font-size: 3rem;"></i>
                    </div>
                    <p class="text-muted">You haven't submitted any service requests yet.</p>
                    <a asp-action="Create" class="btn btn-primary">Submit Your First Request</a>
                </div>
            }
        </div>
    </div>
</div> 